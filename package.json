{
  "name": "silquess-links",
  "version": "1.0.0",
  "description": "Silquess Links - Refined fashion for modern minimalists",
  "main": "index.html",
  "scripts": {
    "dev": "python3 -m http.server 3000 || python -m http.server 3000 || npx serve -s . -l 3000",
    "start": "python3 -m http.server 3000 || python -m http.server 3000 || npx serve -s . -l 3000",
    "build": "echo 'Static HTML project - no build needed'",
    "serve": "npx serve -s . -l 3000"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  },
  "keywords": [
    "static",
    "html",
    "fashion",
    "links"
  ],
  "author": "Silquess",
  "license": "MIT"
}
